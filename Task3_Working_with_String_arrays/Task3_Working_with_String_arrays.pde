
void setup(){
  
  String[] musicArtists = { "Tool", "Depeche Mode", "Korn", "Slipnot", "The avalances"};
  String[] hits = {"hammer", "Christ", "Bad", "Badder", "Phyco"};
  for (int i = 0; i < musicArtists.length; i++){
  println(i + 1+ ". " + musicArtists[i] + " : " + hits [i]);
    
  }
}
  
